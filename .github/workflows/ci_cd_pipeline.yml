name: CI/CD Pipeline - Train & Build Docker

on:
  push:
    paths:
      - 'MLProject/**' 

env:
  MLFLOW_TRACKING_URI: ${{ secrets.MLFLOW_TRACKING_URI }}
  MLFLOW_TRACKING_USERNAME: ${{ secrets.MLFLOW_TRACKING_USERNAME }}
  MLFLOW_TRACKING_PASSWORD: ${{ secrets.MLFLOW_TRACKING_PASSWORD }}

jobs:
  train-and-build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set up Python
      id: setup-python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install MLflow & Conda
      run: |
        pip install "mlflow==2.22.0" dagshub boto3
        wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh
        bash miniconda.sh -b -p $HOME/miniconda
        echo "$HOME/miniconda/bin" >> $GITHUB_PATH

    - name: Retrain Model with MLflow Project
      id: training
      run: |
        # Eksekusi MLflow Project (Akan otomatis memanggil conda.yaml)
        mlflow run MLProject/ --experiment-name "CreditCard_Fraud_Detection_Production"
        
        PYTHON_EXE="${{ steps.setup-python.outputs.python-path }}"
        
        # Ambil Run ID utama (Parent Run) menggunakan script Python stabil
        LATEST_RUN_ID=$($PYTHON_EXE -c "
        import mlflow
        client = mlflow.tracking.MlflowClient()
        try:
            exp = client.get_experiment_by_name('CreditCard_Fraud_Detection_Production')
            # Filter agar tidak mengambil nested run evaluasi
            runs = client.search_runs(
                experiment_ids=[exp.experiment_id],
                filter_string='attributes.run_name != \"Manual_Evaluation_Artifacts\"',
                order_by=['start_time DESC'],
                max_results=1
            )
            print(runs[0].info.run_id)
        except Exception as e:
            print(f'Error fetching run id: {e}')
            import sys; sys.exit(1)
        ")
        
        echo "Parent Run ID: $LATEST_RUN_ID"
        echo "run_id=$LATEST_RUN_ID" >> $GITHUB_OUTPUT
        
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and Push Docker Image
      env:
        RUN_ID: ${{ steps.training.outputs.run_id }}
        DOCKER_REPO: ${{ secrets.DOCKER_USERNAME }}/fraud-detection-model
      run: |
        echo "Building Docker Image for Run ID: $RUN_ID"
        
        # Cari folder model di seluruh workspace runner GitHub.
        # Ini menghindari download dari DagsHub (bypass error 500).
        LOCAL_MODEL_PATH=$(find . -name "model" -type d | grep "$RUN_ID" | head -n 1)
        
        if [ -n "$LOCAL_MODEL_PATH" ]; then
          echo "Model ditemukan secara lokal: $LOCAL_MODEL_PATH"
          MODEL_URI=$LOCAL_MODEL_PATH
        else
          echo "Model lokal tidak ditemukan. Fallback ke remote (berisiko error 500 DagsHub)..."
          MODEL_URI="runs:/$RUN_ID/model"
        fi
        
        # Build Docker Image dari path lokal (jika ketemu) atau remote URI
        mlflow models build-docker \
          -m "$MODEL_URI" \
          -n "$DOCKER_REPO:latest" \
          --enable-mlserver
        
        docker push "$DOCKER_REPO:latest"
        echo "Docker Image Pushed Successfully!"
